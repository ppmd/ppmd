[compiler]
name = DPCPP
binary = dpcpp
compile-flags = -fPIC -qopt-report=5 -m64 -qopenmp -lc -lrt -g -m64
link-flags = -lm
opt-flags = -O3 -xHost
debug-flags = -g
compile-object-flag = -c
shared-object-flag = -shared
restrict-keyword = 
