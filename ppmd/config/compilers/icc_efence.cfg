[compiler]
name = ICC_EFENCE
binary = icc
compile-flags = -fPIC -std=c++0x -restrict -qopt-report=5 -m64 -qopenmp -lgomp -lpthread -lc -lrt -g
link-flags = -lm -lefence
opt-flags = -O3 -xHost
debug-flags = -g
compile-object-flag = -c
shared-object-flag = -shared
restrict-keyword = restrict
