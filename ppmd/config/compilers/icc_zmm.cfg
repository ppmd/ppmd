[compiler]
name = ICC_ZMM
binary = icc
compile-flags = -fPIC -std=c++0x -restrict -qopt-report=5 -m64 -qopenmp -lc -lrt -g
link-flags = -lm
opt-flags = -O3 -xHost -qopt-zmm-usage=high
debug-flags = -g
compile-object-flag = -c
shared-object-flag = -shared
restrict-keyword = restrict
